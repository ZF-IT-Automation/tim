# docs: present TIM features, strengths and honest memory comparisons

## What to build

Visitors can understand TIM's value, install from source, connect an agent and compare its project/session focus with representative memory alternatives using dated primary sources.

## Blocked by

None — can start immediately.

## Seam

README rendering, exact CLI --help contracts, primary-source comparison audit.

## Acceptance criteria

- [ ] Rewrite README with feature groups, concrete workflows, working commands, architecture and maintenance links.
- [ ] Highlight local ownership, cross-harness continuity, original sources plus summaries, structured project work and inspectability.
- [ ] Cite representative MCP, Mem0/OpenMemory, Claude Code and OpenAI official documentation; acknowledge shared strengths rather than claiming uniqueness.
- [ ] Clearly separate shipped capabilities, current beta limitations and planned extensions; keep feature status aligned after implementation.
- [ ] Fix obsolete/missing documentation links and incorrect build instructions; publish to GitHub with tested local links.

## Constraints

Keep Node.js 22 compatibility and existing public defaults unless the acceptance criteria explicitly change them. Use existing packages and metadata before adding dependencies or schema. Preserve original exchanges. No production database maintenance, credential changes, hosted deployment, or unrelated refactors. Add tests at the stated seam. Implement in an isolated full clone, then commit and report verification for independent integration review.

**Status:** ready-for-agent

