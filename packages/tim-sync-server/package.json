{
  "name": "tim-sync-server",
  "version": "0.1.0-beta.1",
  "description": "TIM hosted sync server — multi-tenant blind sync with quotas",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "tim-sync-server": "dist/cli.js"
  },
  "files": [
    "dist/**/*",
    "!dist/**/__tests__/**"
  ],
  "scripts": {
    "build": "tsc",
    "pretest": "npm --prefix ../.. run build",
    "test": "vitest run"
  },
  "dependencies": {
    "tim-core": "0.1.0-beta.1",
    "better-sqlite3": "^11.9.1"
  }
}
