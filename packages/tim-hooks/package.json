{
  "name": "tim-hooks",
  "version": "0.1.0-beta.1",
  "description": "TIM hook scripts and checkpoint orchestration",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/**/*",
    "!dist/**/__tests__/**",
    "scripts/**/*"
  ],
  "scripts": {
    "build": "tsc",
    "pretest": "npm --prefix ../.. run build",
    "test": "vitest run"
  },
  "dependencies": {
    "tim-core": "0.1.0-beta.1",
    "tim-store": "0.1.0-beta.1"
  }
}
